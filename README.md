# Renne Pet

Renne is a Codex pet package with a static GitHub Pages preview.

## Files

- `pet.json` contains the pet package metadata.
- `spritesheet.webp` contains the transparent 8-column by 9-row animation atlas.
- `index.html` previews every animation state directly from the spritesheet.
- `qa/` contains validation and review artifacts from the pet build.

## Preview

Open `index.html` in a browser, or use the GitHub Pages URL after the repository is published.

The preview includes all packaged animation states:

- idle
- running-right
- running-left
- waving
- jumping
- failed
- waiting
- running
- review
